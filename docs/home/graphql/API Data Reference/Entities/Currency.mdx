We use the [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) standard for
defining currencies. Examples are USD, GBP, EUR or SEK.

Example:

```javascript
{
    "priceCurrency": "USD"
}
```

Valid options:

```javascript
enum Currency {
    EUR = 'EUR',
    NOK = 'NOK',
    USD = 'USD',
}
```
