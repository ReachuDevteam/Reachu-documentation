<font color="grey" size="4">

Represents core data of a channel

</font>

```javascript
type Channel {
  id: ID
  name: String
  enabled: Boolean
}
```

#### Fields

**`id (ID)`**

**`name (String)`**

**`enabled (Boolean)`**

Defines if the channel is currently active or not.